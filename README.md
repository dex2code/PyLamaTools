# PyLamaTools

Простой чат-оркестратор с возможностью расширения локальных инструментов.

---

## Требования

- **Ollama** – сервер с запущенной моделью (API URL задаётся в `config.py`).
- **Python 3.10+** – для создания виртуального окружения.
- **Git** – для клонирования репозитория.
- Очень рекомендуется модель семейства qwen3.5+ как максимально подходящая для работы с инструментами

---

## Установка и запуск

Выполните последовательно команды в терминале:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/dex2code/PyLamaTools.git

# 2. Перейдите в папку проекта
cd ./PyLamaTools

# 3. Создайте виртуальное окружение
python3 -m venv ./.venv

# 4. Активируйте виртуальное окружение
source ./.venv/bin/activate

# 5. Обновите pip до актуальной версии
pip install --upgrade pip

# 6. Установите зависимости
pip install -r ./requirements.txt

# 7. Запустите приложение
python3 -m main
```

---

## Настройка

1. Отредактируйте файл `config.py`, указав корректный URL вашего сервера **Ollama** (по умолчанию: `http://localhost:11434`).
2. При необходимости измените другие параметры (модель, порт и т.д.).

---

## Примечания

- При первом запуске убедитесь, что сервер **Ollama** запущен и доступен.
- Для остановки приложения нажмите `Ctrl+C`.
- Все зависимости перечислены в `requirements.txt` – их установка обязательна.

---

# Пример использования

$ python -m main
<pre>
✅ Инициализация завершена:
  🤖 Инструментов: 3
  🤝 API: http://127.0.0.1:11434
  🧠 Модель: qwen3.5:9b
  💬 Системный промт (токенов): 758
</pre>
<pre>
✨ Этот чат работает с языковой моделью, которая умеет выполнять полезные действия:
инструментарий находится в каталоге tools и вы можете расширять его самостоятельно.
Просто задайте вопрос на русском языке — например, «Какая сейчас погода?».
Модель сама решит, когда нужно вызвать инструмент и ответит полученным значением.
Чтобы узнать, что умеет модель - спросите: "Что ты умеешь?".
Если хотите закончить — напишите 'exit' или 'выход'.
</pre>
<pre>
👤 Вы: Привет, что ты умеешь?
</pre>
<pre><i>
⏳ Пользователь спрашивает, что я умею.
Нужно кратко перечислить доступные инструменты и их назначение на основе того, что передано в описании.
У меня есть следующие инструменты:
1. read_file_content.read_file_content - чтение текстовых файлов
2. find_files_and_dirs.find_files_and_dirs - поиск файлов и каталогов по glob-паттернам
3. get_weather.get_weather - получение погоды по геолокации (IP)
Нужно ответить кратко и понятно на русском языке, перечислив возможности.
</i></pre>
🤖 qwen3.5:9b: Я — ИИ-ассистент со следующими возможностями:

1. **Чтение файлов** — могу прочитать содержимое текстовых файлов по заданному пути (поддерживаются разные кодировки и ограничение по объёму).

2. **Поиск файлов и каталогов** — могу рекурсивно или не рекурсивно искать файлы в директории по glob-шаблонам (например, `*.txt`, `log[0-9].log`).

3. **Погода** — могу узнать текущую погоду относительно местоположения клиента (по IP-адресу), возвращается температура и влажность.

Если у вас есть задача, требующая использования одного из этих инструментов — обращайтесь!
   Размер контекста: 1079 токенов

👤 Вы: Что мне надеть на прогулку?
⏳ Пользователь спрашивает о том, что надеть на прогулку. Это зависит от погоды в его местоположении. Я могу использовать функцию get_weather для получения текущей погоды по IP-адресу пользователя, которая вернёт температуру и влажность. Эта информация поможет дать рекомендации по одежде.

Я вызову инструмент для получения погодных данных.
⚙️  Вызов инструмента 'get_weather.get_weather' с аргументами {}
⏳ Пользователь спрашивает, что надеть на прогулку. Я вызвал инструмент для получения погоды, и получил результат: температура 17°C, влажность 53%.

На основе этих данных я могу дать рекомендацию по одежде. Температура 17°C - это прохладная погода, умеренная влажность. Нужно посоветовать соответствующую одежду.

Не нужно вызывать дополнительные инструменты, так как я получил данные о погоде и могу ответить на вопрос.
🤖 qwen3.5:9b: Сейчас температура составляет **17°C**, влажность — **53%**. Это прохладная погода для прогулки. Рекомендую:

- Верхняя одежда: легкая куртка, ветровка или пуховик
- Базовый слой: джемпер, водолазка или толстовка
- Брюки или длинные юбки (лёгкие ткани подойдут)
- Обувь: кроссовки или ботинки на нескользящей подошве

Также можно взять шарф и шапку — особенно если планируется вечерняя прогулка!
   Размер контекста: 1361 токенов
